// eslint-disable-next-line no-unused-vars
import React from 'react';
import Spacial from '../Special/Spacial';

// eslint-disable-next-line react/prop-types
const Myself = ({ring}) => {
    return (
        <div>
            <h2>Myself</h2>
            <Spacial ring={ring}></Spacial>
        </div>
    );
};

export default Myself;